---
interface Props {
    icon: string,
    link: string,
    title: string,
}

const { icon, link, title } = Astro.props;
---

<a href={link} title={title} aria-label={title}><i class={`bi-${icon}`} /></a>